{: NB. monad
_2 & {.
4 : 'y {~ <: x'
# NB. monad
|. NB. monad
-: |. NB. monad
NB.
3 : 'y #~ }: padded ~: 1 |. padded =. y , a. {~ >: a. i. {: y'
NB.
NB.
NB.
NB.
NB.
2 & # NB. monad
# NB. dyad
4 : 'y #~ 0 ~: x | >: i. # y'
< @: {. , < @: }. NB. fork, equivalent to (< @: {.) , (< @: }.)
NB. do this later, not that hard
|. NB. dyad
4 : '((<: x) {. y) , x }. y'
NB. do this later, not that hard
a =. 4 : 'x + i. >: y - x'
